import Sort from './Sort'
import Filter from './Filter'

function Sidebar() {

    return (
        <div className="sidebar">
            <Sort/>
            <Filter/>
        </div>
    );
}

export default Sidebar;